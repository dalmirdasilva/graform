%p
  = @question.statement

.btn-group.pull-right
  = link_to 'Edit', edit_form_question_url(@form, @question), { class: 'btn btn-mini' }
  = link_to 'All', form_questions_url(@form), { class: 'btn btn-mini' }
