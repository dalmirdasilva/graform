= form_for @reply, url: form_reply_path(@form, @reply), html: { class: 'form-horizontal'} do |f|
  .field.control-group
    .controls
      .btn-group
        = f.submit 'Save', { class: 'btn btn-small' }
        = link_to 'Cancel', form_replies_path(@form), { class: 'btn btn-small' }
