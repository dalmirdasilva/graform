= form_for @question_type, url: question_type_url(@question_type), html: { class: 'form-horizontal'} do |f|
  .field.control-group
    = f.label :code, class: 'control-label'
    .controls
      = f.number_field :code, class: 'span2'
  .field.control-group
    = f.label :name, class: 'control-label'
    .controls
      = f.text_field :name, class: 'span5'
  .field.control-group
    .controls
      .btn-group
        = f.submit t('save'), { class: 'btn btn-small' }
        = link_to t('cancel'), question_types_url, { class: 'btn btn-small' }
