%section.editor_side_bar
  %h4 Adicionar questão
  %ul
    - @question_types.each do |question_type|
      %li
        %button.btn.btn-default.btn-snall.add-question{:question_type_id => question_type.id}
          Adicionar
        = t("title.question_types.names.#{question_type.name}")

  %h4 
  %ul
    %li
      Link para respoder esse formulário:
      = link_to "#{::HOST_AND_PORT}#{@form.public_link}", @form.public_link
      
