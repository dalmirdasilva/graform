- flash.each do |name, msg|
  - messages = msg.is_a?(Array) ? msg : [msg]
  - messages.each do |msg|
    %div{:class => "alert alert-#{name == :notice ? "success" : "error"}"}
      %a.close{"data-dismiss" => "alert"} ×
      = content_tag :div, msg, id: "flash_#{name}"